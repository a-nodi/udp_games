using UnityEngine;

public enum EVENT_TYPE
{
    // Animation Events
    WALK,
    RUN,
    FALL,
    FOUND,
    JUMP,

    // Game Events
    GET_SCORE,
    GAME_START,
    GAME_OVER,

    // UI Events
    UPDATE_SCORE,


}